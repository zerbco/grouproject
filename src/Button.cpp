#include "Button.h"

// Button::Button(void (*func)(), float x, float y , float width , float height): RenderableRect(x, y ,width,height), func(func){}

// void Button::click(sf::Vector2u point){
//     if (collide(point)) {
//         func();
//     }
// }